@page "/forms/AddStorageSystem"

@if (CommonFuncs.CanView(navLink))
{
    <ResourceFormHandler Cipher_TItem="IStorageSystem" NavLink="@navLink" Mode="@FormMode.Create" />
}

@code {
    private CipherNavLink navLink = CipherNavLinks.AddStorageSystem;
}