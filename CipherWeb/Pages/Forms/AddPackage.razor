@page "/forms/AddPackage"

@if (CommonFuncs.CanView(navLink))
{
    <ResourceFormHandler Cipher_TItem="IPackage" NavLink="@navLink" Mode="@FormMode.Create" />
}

@code {
    private CipherNavLink navLink = CipherNavLinks.AddPackage;
}