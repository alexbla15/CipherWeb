@page "/forms/AddProcess"

@inject NotificationService NotificationService
@inject ICipherInfo _db

<PageTitle>תהליך חדש</PageTitle>

<CipherSteps>
    <RadzenStepsItem Text="פרטי הזנה">
        <CipherFormInfo Title="וודאו את פרטי ההזנה" />

        <CipherComponentCard Title="פרטי התהליך" ContentDisplay="flex" ContentJustify="flex-start" ContentFlexWrap="wrap">
            @* -- Name -- *@
            <CipherAutoComplete Icon="@Icons.Cipher.Id" Label="שם תהליך" />

            <CipherMaterialType />
            <CipherMainCategory />
            <CipherMask/>

            @* -- Default destination -- *@
            <CipherAutoComplete Label="ייעוד" Icon="@Icons.Cipher.Process" />
        </CipherComponentCard>

        <CipherComments Title="הערות על הקטגוריה" />
    </RadzenStepsItem>

    <RadzenStepsItem Text="סיכום ואישור">
        <CipherSubmit ActionText="נפתחה קטגוריה תמיסה לחומר מגנזיום. קטגוריה זו תקבל את הסדרה 111."></CipherSubmit>
    </RadzenStepsItem>
</CipherSteps>