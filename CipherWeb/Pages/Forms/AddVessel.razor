@page "/forms/addvessel"

@inject NotificationService NotificationService

<PageTitle>כלי חדש</PageTitle>

<CipherComponentCard Underline="true" WithCard="false" Title="הזנת כלי חדש" ContentDisplay="flex" ContentJustify="flex-start" ContentFlexWrap="wrap">
    <CipherComponentCard Title="פרטי הכלי" ContentDisplay="flex" ContentJustify="flex-start" ContentFlexWrap="wrap">
        @* -- Name -- *@
        <CipherAutoComplete Icon="@Icons.Cipher.Id" @bind-Value="ves.Id" Label="שם הכלי" />

        <CipherVesselType />
        <CipherLocation Label="מיקום הכלי" />
        <CipherMaterialType />
        <CipherCategoryDropDown />
    </CipherComponentCard>

    <CipherComments Title="כתבו הערות על הכלי" />
    <CipherSubmit ActionText="כלי בשם A1 נפתח במעבדה B. הכלי יכיל עד 5 גרם מגנזיום מסוג תמיסת מגנזיום."></CipherSubmit>
</CipherComponentCard>

@code {
    private Vessel ves = Vessel.Random();
}