@page "/forms/AddVessel"

@if (CommonFuncs.CanView(navLink))
{
    <ResourceFormHandler Cipher_TItem="IVessel" NavLink="@navLink" Mode="@FormMode.Create" />
}

@code {
    private CipherNavLink navLink = CipherNavLinks.AddVessel;
}