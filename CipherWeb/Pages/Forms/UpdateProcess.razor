@page "/Forms/Updates/Process"

@if (CommonFuncs.CanView(navLink))
{
    <ResourceFormHandler Cipher_TItem="IProcessDefinition" NavLink="@navLink" />
}

@code {
    private CipherNavLink navLink = CipherNavLinks.UpdateProcess;
}
