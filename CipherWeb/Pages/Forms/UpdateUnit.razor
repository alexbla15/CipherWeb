@page "/Forms/Updates/Unit"

@if (CommonFuncs.CanView(navLink))
{
    <ResourceFormHandler Cipher_TItem="IUnit" NavLink="@navLink" />
}

@code {
    private CipherNavLink navLink = CipherNavLinks.UpdateUnit;
}