@page "/forms/AddUnit"

@if (CommonFuncs.CanView(navLink))
{
    <ResourceFormHandler Cipher_TItem="IUnit" NavLink="@navLink" Mode="@FormMode.Create" />
}

@code {
    private CipherNavLink navLink = CipherNavLinks.AddUnit;
}