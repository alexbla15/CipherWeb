@page "/Forms/Updates/System"

@if (CommonFuncs.CanView(navLink))
{
    <ResourceFormHandler Cipher_TItem="IStorageSystem" NavLink="@navLink" />
}

@code {
    private CipherNavLink navLink = CipherNavLinks.UpdateSystem;
}