@page "/forms/AddCategory"

@if (CommonFuncs.CanView(navLink))
{
    <ResourceFormHandler Cipher_TItem="ICategory" NavLink="@navLink" Mode="@FormMode.Create" />
}

@code {
    private CipherNavLink navLink = CipherNavLinks.AddCategory;
}