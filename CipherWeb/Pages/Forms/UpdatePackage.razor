@page "/Forms/UpdatePackage"

@inject ICipherInfo _db

<PageTitle>עדכון תעודה</PageTitle>

<CipherSteps>
    <RadzenStepsItem Text="פרטי הזנה">
        <CipherFormInfo Title="וודאו את פרטי ההזנה"/>
        <CipherComponentCard Title="חפשו את התעודה הרלוונטית, ועדכנו את הפרטים שלה בהתאם לצורך">
            <CipherSearchRow JustifyRow="JustifyContent.Start" OnKeyDown="@SearchFunc" />
        <br/>
        <CipherPackageInfo WithCard="false" pack="new Package()"/>
        </CipherComponentCard>
        <CipherComments Title="כתבו הערות על התנועה"/>
    </RadzenStepsItem>

    <RadzenStepsItem Text="סיכום ואישור">
        <CipherSubmit ActionText="אבי רון עדכן את מסת התעודה מ5 גרם ל4.9 גרם בתאריך 14.7.24."/>
    </RadzenStepsItem>
</CipherSteps>

@code
{
    private void SearchFunc(KeyboardEventArgs args)
    {
    }
}