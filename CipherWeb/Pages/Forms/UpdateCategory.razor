@page "/Forms/Updates/Category"

@if (CommonFuncs.CanView(navLink))
{
    <ResourceFormHandler Cipher_TItem="ICategory" NavLink="@navLink" />
}

@code {
    private CipherNavLink navLink = CipherNavLinks.UpdateCategory;
}