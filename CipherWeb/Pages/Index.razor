@page "/"

@inject TooltipService tooltipService

@if (CommonFuncs.CanView(navLink))
{
    <PageTitle>@navLink.Name</PageTitle>

    <CipherTopNavBar />
    <CipherProcessesScheduler />

    @if (Constants.SetUser.Group == WorkerGroup.Manager)
    {
        <CipherPackagesTable AllowFilter="false" AllowGroup="false" Title="סיכום חומרים" />
    }
}

@code {
    private CipherNavLink navLink = CipherNavLinks.Home;
}