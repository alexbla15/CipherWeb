@page "/"

@inject ICipherInfo _db
@inject TooltipService tooltipService
@inject ExcelService excelService

<PageTitle>מסך פתיחה</PageTitle>

<CipherTopNavBar/>

@if (Roles.CurrnetRole == Roles.SysManager)
{
    <RadzenRow>
        <CipherProcessesScheduler/>
    </RadzenRow>
    <RadzenRow>
        <RadzenColumn Size="12">
            <CipherPackagesTable AllowFilter="false" AllowGroup="false" Title="סיכום חומרים" RowsPerPage=@rowsPerPage/>
        </RadzenColumn>
    </RadzenRow>
}
else
{
    <RadzenRow Gap="0">
        <CipherProcessesScheduler />
    </RadzenRow>
}

@code {
    private int rowsPerPage = 3;
}