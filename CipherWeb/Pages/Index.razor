@page "/"

@using System.Text

@inject ICipherInfo _db
@inject TooltipService tooltipService
@inject ExcelService excelService

<PageTitle>מסך פתיחה</PageTitle>

<CipherTopNavBar/>

<br />

@if (Roles.CurrnetRole == Roles.SysManager)
{
    <RadzenRow>
        <CipherEventsScheduler />
    </RadzenRow>
    <RadzenRow>
        <RadzenColumn Size="6">
            <CipherPackagesTable AllowFilter="false" AllowGroup="false" Title="סיכום חומרים" Height="230px" RowsPerPage="3"/>
        </RadzenColumn>
        <RadzenColumn Size="6">
            <CipherLocationsTable AllowFilter="false" AllowGroup="false" Height="230px" RowsPerPage="3" />
        </RadzenColumn>
    </RadzenRow>
}
else
{
    <RadzenRow Gap="0">
        <CipherEventsScheduler />
    </RadzenRow>
}

@code {
    int EventsInSlot = 3;
}