@page "/"

@inject ICipherInfo _db
@inject TooltipService tooltipService
@inject ExcelService excelService

<PageTitle>מסך פתיחה</PageTitle>

<CipherTopNavBar/>

@if (Roles.CurrnetRole == Roles.SysManager)
{
    <RadzenRow>
        <CipherEventsScheduler />
    </RadzenRow>
    <RadzenRow>
        <RadzenColumn Size="12">
            <CipherPackagesTable AllowFilter="false" AllowGroup="false" Title="סיכום חומרים" Height="@cardHeight" RowsPerPage=@rowsPerPage />
        </RadzenColumn>
    </RadzenRow>
}
else
{
    <RadzenRow Gap="0">
        <CipherEventsScheduler />
    </RadzenRow>
}

@code {
    string cardHeight = "240px";
    int rowsPerPage = 3;
}