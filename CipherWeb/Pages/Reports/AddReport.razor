@page "/Reports/Add"

@if (CommonFuncs.CanView(navLink))
{
    <PageTitle>@navLink.Name</PageTitle>

    <CipherReportWizard />
}

@code {
    private CipherNavLink navLink = CipherNavLinks.AddReport;
}