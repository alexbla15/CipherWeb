@page "/Reports/Events"

@if (CommonFuncs.CanView(navLink))
{
    <PageTitle>@navLink.Name</PageTitle>

    <CipherEventsTable Sharable="true" IsReport="true" />
}

@code {
    private CipherNavLink navLink = CipherNavLinks.EventsReport;
}

