@page "/Reports/Packages"

@if (CommonFuncs.CanView(navLink))
{
    <PageTitle>@navLink.Name</PageTitle>

    <CipherPackagesTable Sharable="true" IsReport="true" />
}

@code {
    private CipherNavLink navLink = CipherNavLinks.PackagesReport;
}