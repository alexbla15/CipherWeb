@page "/Searches/PackageSearch"

@using CipherWeb.Pages.Forms

@if (CommonFuncs.CanView(navLink))
{
    <ResourceFormHandler NavLink="@navLink" Cipher_TItem="IPackage" Mode="@FormMode.ReadOnly" />
}

@code {
    private CipherNavLink navLink = CipherNavLinks.PackageSearch;
}
