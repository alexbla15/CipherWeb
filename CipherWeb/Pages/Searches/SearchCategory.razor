@page "/Searches/CategorySearch"

@using CipherWeb.Pages.Forms

@if (CommonFuncs.CanView(navLink))
{
    <ResourceFormHandler NavLink="@navLink" Cipher_TItem="ICategory" Mode="@FormMode.ReadOnly" />
}

@code {
    private CipherNavLink navLink = CipherNavLinks.CategorySearch;
}