@page "/Searches/SystemSearch"

@using CipherWeb.Pages.Forms

@if (CommonFuncs.CanView(navLink))
{
    <ResourceFormHandler NavLink="@navLink" Cipher_TItem="IStorageSystem" Mode="@FormMode.ReadOnly" />
}

@code {
    private CipherNavLink navLink = CipherNavLinks.SystemSearch;
}