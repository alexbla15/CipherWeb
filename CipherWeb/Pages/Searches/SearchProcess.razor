@page "/Searches/ProcessSearch"

@using CipherWeb.Pages.Forms

@if (CommonFuncs.CanView(navLink))
{
    <ResourceFormHandler NavLink="@navLink" Cipher_TItem="IProcessDefinition" Mode="@FormMode.ReadOnly" />
}

@code {
    private CipherNavLink navLink = CipherNavLinks.ProcessSearch;
}