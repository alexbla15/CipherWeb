@page "/Searches/AdvancedSearch"

@if (CommonFuncs.CanView(navLink))
{
    <PageTitle>@navLink.Name</PageTitle>

    <CipherFilterObjects />
}

@code {
    private CipherNavLink navLink = CipherNavLinks.AdvancedSearch;
}