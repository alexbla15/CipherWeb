@code {
    /// <summary>
    /// Is component visible on page
    /// </summary>
    [Parameter]
    public bool Visible { get; set; } = Constants.CheckJsons;

    /// <summary>
    /// Json value.
    /// </summary>
    [Parameter]
    public string JsonValue { get; set; } = string.Empty;
}

@if (Visible)
{
    <CipherComponentCard Title="בדיקה">
        <RadzenTextArea @bind-Value="JsonValue" ReadOnly="true" Style="width: 100%; direction:ltr; height: 300px" />
    </CipherComponentCard>
}