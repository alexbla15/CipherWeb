@inherits CipherResourceTable<Category>

<CipherResourceTable Cipher_TItem="Category" ChosenObjects="@ChosenObjects" PrefixFileTitle="categories" Title="@Title" WithHeaderContent="false"/>

@code {
    protected override void OnInitialized()
    {
        Title ??= Translator.GetTranslation("Categories");
        ChosenObjects ??= CachedData.AllCategories.Item1;
    }
}