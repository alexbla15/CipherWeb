@inherits CipherResourceTable<ICategory>

<CipherResourceTable Cipher_TItem="ICategory" ChosenObjects="@CachedData.AllCategories.Item1" PrefixFileTitle="categories" Title="@Title" WithHeaderContent="false" />

@code {
    protected override void OnInitialized()
    {
        Title ??= Translator.GetTranslation("Categories");
    }
}