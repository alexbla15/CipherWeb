@inherits CipherAutoComplete

<CipherAutoComplete Icon="@Icons.Person" Label="@Label" Data="@TestedData.Workers" Disabled="@Disabled" Value="@Constants.SetUser.FullName()" Width="@Width"/>

@code {
    protected override void OnInitialized()
    {
        Label ??= "ממלא הטופס";
    }
}