@inherits CipherAutoComplete

<CipherAutoComplete Label="@Label" Icon="@Icon" MinLength="1" MaxLength="3" />

@code {
    protected override async Task OnInitializedAsync()
    {
        Label ??= " (***)";
        Icon ??= Icons.Security.Password;
    }
}