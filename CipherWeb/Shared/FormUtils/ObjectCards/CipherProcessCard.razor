@inherits CipherComponentCard

@code {
    /// <summary>
    /// Width of each row within card.
    /// </summary>
    [Parameter]
    public string RowWidth { get; set; } = "300px";

    public class Analysis
    {
        public int Id { get; set; }
        public string Type { get; set; }
        public decimal Value { get; set; }
        public DateTime EventDate { get; set; }
    }

    private List<Analysis> analysis = new();
}

<CipherComponentCard Title="@Title" WithCard="@WithCard" Width="1000px" MaxWidth="1000px">
    <RadzenTabs>
        <Tabs>
            <RadzenTabsItem Text="פרטים">
                @* -- ID -- *@
                <CipherAutoComplete Label="מספר סידורי" Icon="@Icons.Cipher.Id" Width="@RowWidth" />

                @* -- Name -- *@
                <CipherAutoComplete Label="שם תהליך" Icon="@Icons.Communication.Chat.QA" Width="@RowWidth" />

                @* -- TYPE -- *@
                <CipherAutoComplete Label="סוג תהליך" Icon="@Icons.Symbols.Category" Width="@RowWidth" />

                @* -- LOCATIONS -- *@
                <CipherAutoComplete Label="מיקומים" Icon="@Icons.Cipher.Location" Width="@RowWidth" />

                @* -- IN PROCESSES -- *@
                <CipherAutoComplete Label="תהליכים יוצרים" Icon="@Icons.Arrows.Squared.Input" Width="@RowWidth" />

                @* -- OUT PROCESSES -- *@
                <CipherAutoComplete Label="תהליכים צורכים" Icon="@Icons.Arrows.Squared.Output" Width="@RowWidth" />

                @* -- IN CATEGORIES -- *@
                <CipherAutoComplete Label="סוגים נצרכים" Icon="@Icons.Arrows.Squared.Input" Width="@RowWidth" />

                @* -- OUT CATEGORIES -- *@
                <CipherAutoComplete Label="סוגים צורכים" Icon="@Icons.Arrows.Squared.Output" Width="@RowWidth" />

                @* -- UPDATE DATE -- *@
                <CipherDatePicker Label="תאריך עדכון" />

                @* -- UPDATING WORKER -- *@
                <CipherWorker Label="עובד/ת מעדכנ/ת" Width="@RowWidth" />

                @* -- AUTHORIZING DATE -- *@
                <CipherDatePicker Label="תאריך אישור" />

                @* -- AUTHORIZING WORKER -- *@
                <CipherWorker Label="עובד/ת מאשר/ת" Width="@RowWidth" />

                @* -- RESTRICTIONS -- *@
                <CipherAutoComplete Label="מגבלות" Icon="@Icons.Documents.Delete.Dangerous" Width="@RowWidth" />

                @* -- EXPIRATION DATE -- *@
                <CipherDatePicker Label="תאריך תפוגה" />

                @* -- COMMENTS -- *@
                <CipherAutoComplete Multiline="true" Label="הערות" Icon="@Icons.Communication.Chat.Comment" Width="@RowWidth"/>

                <br />
                <br />
            </RadzenTabsItem>

            <RadzenTabsItem Text="תהליכי עבר">
                <CipherEventsTable Title="" WithCard="false" />
            </RadzenTabsItem>

            <RadzenTabsItem Text="תעודות">
                <CipherPackagesTable Title="" WithCard="false" />
            </RadzenTabsItem>
        </Tabs>
    </RadzenTabs>

</CipherComponentCard>