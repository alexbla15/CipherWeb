@inherits CipherComponentCard

@code {
    /// <summary>
    /// Width of component card.
    /// </summary>
    [Parameter]
    public string CardWidth { get; set; } = "1000px";

    /// <summary>
    /// Maximal width of component card.
    /// </summary>
    [Parameter]
    public string CardMaxWidth { get; set; } = "1000px";

    /// <summary>
    /// Set editability of each row.
    /// </summary>
    [Parameter]
    public bool ReadOnly { get; set; } = false;
}

<CipherComponentCard Title="@Title" WithCard="@WithCard" Width="@CardWidth" MaxWidth="@CardMaxWidth">
    <RadzenTabs>
        <Tabs>
            <RadzenTabsItem Text="פרטים">
                <CipherAutoComplete Icon="@Icons.Cipher.Id" Label="מספר סידורי" Disabled="@ReadOnly" />
                <CipherAutoComplete Icon="@Icons.Cipher.Type" Label="סוג תנועה" Disabled="@ReadOnly" />
                <CipherAutoComplete Icon="@Icons.Cipher.Process" Label="תהליך" Disabled="@ReadOnly" />
                <CipherDatePicker Label="תאריך תנועה" Disabled="@ReadOnly" />
                <CipherAutoComplete Icon="@Icons.Cipher.Account_Old" Label="עובד מעדכן" Disabled="@ReadOnly" />
                <CipherDatePicker Label="תאריך עדכון" Disabled="@ReadOnly" />
                <CipherAutoComplete Icon="@Icons.Cipher.Account_New" Label="עובד מאשר" Disabled="@ReadOnly" />
                <CipherDatePicker Label="תאריך אישור" Disabled="@ReadOnly" />
                <CipherAutoComplete Icon="@Icons.UiComponents.settings" Label="פרמטרים" Disabled="@ReadOnly" />
                <CipherAutoComplete Icon="@Icons.Cipher.Status" Label="סטטוס" Disabled="@ReadOnly" />
                <CipherAutoComplete Multiline="true" Label="הערות" Icon="@Icons.Communication.Chat.comment" Disabled="@ReadOnly" />

                <br/>
                <br/>
            </RadzenTabsItem>
        </Tabs>
    </RadzenTabs>

</CipherComponentCard>