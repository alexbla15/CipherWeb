@inherits CipherComponentCard

@code {
    [Parameter]
    public string RowWidth { get; set; } = "width: 400px";

    [Parameter]
    public bool ReadOnly { get; set; } = false;

    public class Analysis
    {
        public int Id { get; set; }
        public string Type { get; set; }
        public decimal Value { get; set; }
        public DateTime EventDate { get; set; }
    }

    List<Analysis> analysis = new();
}

<CipherComponentCard Title="@Title" WithCard="@WithCard">
    <RadzenTabs>
        <Tabs>
            <RadzenTabsItem Text="פרטים">
                <RadzenRow>
                <RadzenColumn>
                        @* -- Vessels ID -- *@
                        <CipherFormRow Label="מספר סידורי" Icon="@Icons.Id" Style="position:fixed">
                            <RadzenAutoComplete Placeholder="מספר סידורי" Style="@RowWidth" Disabled="@ReadOnly" />
                        </CipherFormRow>

                        @* -- VESSEL NAME -- *@
                        <CipherFormRow Label="שם הכלי" Icon="@Icons.Vessel" Style="position:fixed">
                            <RadzenAutoComplete Placeholder="כלי" Style="@RowWidth" Disabled="@ReadOnly" />
                        </CipherFormRow>

                        @* -- OPEN DATE -- *@
                        <CipherDatePicker Label="תאריך פתיחה" Disabled="@ReadOnly" />

                        @* -- RESTRICTIONS -- *@
                        <CipherFormRow Label="מגבלות" Icon="@Icons.Restriction" Style="position:fixed">
                            <RadzenAutoComplete Placeholder="מגבלות איחסון" Style="@RowWidth" Disabled="@ReadOnly" />
                        </CipherFormRow>

                </RadzenColumn>
                <RadzenColumn>
                    @* -- VESSEL TYPE -- *@
                        <CipherFormRow Label="סוג כלי" Icon="@Icons.Type" Style="position:fixed">
                            <RadzenAutoComplete Placeholder="סוג כלי" Style="@RowWidth" Disabled="@ReadOnly" />
                    </CipherFormRow>

                        @* -- LOCATION -- *@
                        <CipherFormRow Label="מיקום" Icon="@Icons.Location" Style="position:fixed">
                            <RadzenAutoComplete Placeholder="מיקום" Style="@RowWidth" Disabled="@ReadOnly" />
                        </CipherFormRow>

                        @* -- EXPIRATION DATE -- *@
                        <CipherDatePicker Label="תאריך תפוגה" Disabled="@ReadOnly" />

                        @* -- COMMENTS -- *@
                        <CipherFormRow Label="הערות" Icon="@Icons.Comment" Style="position:fixed">
                            <RadzenTextBox Placeholder="הערות" Style="@($"{Constants.textStyle}; {RowWidth}")" Disabled="@ReadOnly" />
                        </CipherFormRow>
                </RadzenColumn>
                </RadzenRow>
        </RadzenTabsItem>

        <RadzenTabsItem Text="תנועות">
                <CipherEventsTable Title="" WithCard="false" />
        </RadzenTabsItem>

            <RadzenTabsItem Text="תעודות">
                <CipherPackagesTable Title="" WithCard="false" />
            </RadzenTabsItem>
        </Tabs>
    </RadzenTabs>

</CipherComponentCard>